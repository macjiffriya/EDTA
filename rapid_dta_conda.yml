name: raipddta
channels:
  - defaults
  - conda-forge
  - pytorch
 

dependencies:
  - python=3.10
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=11.8  # or change to 12.1 if needed
  - pandas
  - numpy
  - scikit-learn
  - pip
  - pip:
      - subword-nmt
     
